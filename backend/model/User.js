class User {
    constructor(userid, username, email, password) {
        this.userid = userid;
        this.username = username;
        this.email = email;
        this.password = password;
    }
}

export default User;